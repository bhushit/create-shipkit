---
description: Stack and project rules for {{name}}
globs: ["**/*.ts", "**/*.tsx"]
---

This is a Hono + Drizzle + Vite/React app deployed on Cloudflare Workers.
Use path aliases (@/) for imports. Run `pnpm lint && pnpm typecheck && pnpm test` before finishing.
Do not change wrangler.toml, CI workflows, or DB schema without asking.
